# ===========================================
# Agentic Trading Backend Dependencies
# ===========================================
# Install: pip install -r requirements.txt
# Python 3.12 required

# -------------------------------------------
# Core Web Framework
# -------------------------------------------
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.10.0
pydantic-settings>=2.6.0

# -------------------------------------------
# LangChain & LangGraph (Agent Framework)
# -------------------------------------------
langchain>=0.3.0
langchain-community>=0.3.0
langchain-openai>=0.2.0
langgraph>=0.2.0

# -------------------------------------------
# Data & Market
# -------------------------------------------
pandas>=2.2.0
numpy>=1.26.0
yfinance>=1.0.0  # KOSPI(^KS11) index_series.py -- regime volatility input

# 음력→양력 변환. krx_holiday/fetcher.py가 설날·부처님오신날·추석을 계산한다
# (손입력 표를 대체). 순수 파이썬·의존성 0·유효 범위 ~2050.
# ⚠️ 없으면 그 연도 달력이 저장되지 않고 untrusted로 남는다(부팅 시 ERROR).
korean_lunar_calendar>=0.3.1

# -------------------------------------------
# Async & HTTP Client
# -------------------------------------------
websockets>=13.0
aiohttp>=3.11.0
httpx>=0.28.0

# -------------------------------------------
# State & Caching
# -------------------------------------------
aiosqlite>=0.20.0
diskcache>=5.6.0
redis[hiredis]>=5.0.0

# -------------------------------------------
# Utilities
# -------------------------------------------
python-dotenv>=1.0.0
structlog>=24.4.0
tenacity>=9.0.0

# -------------------------------------------
# Telegram Notifications
# -------------------------------------------
python-telegram-bot~=22.5

# -------------------------------------------
# Reports (HTML templates)
# -------------------------------------------
# services/reports/render.py -- 전이 의존으로만 깔려 있었다(2026-08-13
# 최종 리뷰 Important 6). 재구축 시 사라지면 import 실패가 never-raise
# 계약에 조용히 삼켜져 리포트 3종(premarket/postmarket/discovery)이
# 전멸한다.
jinja2>=3.1

# -------------------------------------------
# Testing
# -------------------------------------------
pytest>=8.0.0
pytest-asyncio>=0.24.0
pytest-timeout>=2.3.0
pytest-cov>=5.0.0

# -------------------------------------------
# Development Tools (optional)
# -------------------------------------------
black>=24.0.0
isort>=5.13.0
mypy>=1.8.0

# -------------------------------------------
# LLM Server Options (install separately)
# -------------------------------------------
# Option 1: vLLM (Windows/Linux with NVIDIA GPU)
#   pip install vllm
#
# Option 2: Ollama (All platforms)
#   Install from https://ollama.ai or:
#   macOS: brew install ollama
#   Windows: Download installer from ollama.ai
#   Linux: curl -fsSL https://ollama.ai/install.sh | sh
