# Backend origin for the vite dev proxy (persistent per-machine override).
# :8000 is often squatted by another app on this machine - run the Jonber
# backend on :8001 and keep this file as-is.
BACKEND_ORIGIN=http://127.0.0.1:8001
